defmodule Whimsy.Systems.Pdf2e.Engine do
  @moduledoc """
  Determine outcomes to uncertain situations.

  General Flow:
  1. PC wants to do something which requires a check or a roll
  2. PC will be asked to attempt a `Check`
  3. Compare `Check` against `Difficulty Class (DC)`
  """
  alias Bandit.HTTP2.FlowControl
end
